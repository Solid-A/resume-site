## Packages
framer-motion | Smooth animations for sections and interactions
lucide-react | Professional icons for the industry theme
react-hook-form | Form handling for the contact section
zod | Schema validation for forms
@hookform/resolvers | Zod resolver for react-hook-form

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
API Endpoints are defined in shared/routes.ts
Images will be handled via Unsplash for backgrounds/stock, but no specific user uploads are configured in this iteration.
